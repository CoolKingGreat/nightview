# Dev-only / ingestion-only deps. Not installed in the production image.
-r requirements.txt

duckdb>=1.1.0
h3>=4.1.0
geonamescache>=2.0.0
prophet>=1.1.6

ruff>=0.7.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
